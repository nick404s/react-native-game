import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    position: "relative",
    margin: 10,
    alignItems: "center",
    justifyContent: "center",
  },
  textStyle: {
    fontSize: 27,

    color: "#b70a94ff",
    position: "absolute",
  },
});
